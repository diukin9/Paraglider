import { Meta, StoryObj } from "@storybook/react";

import { AuthCard } from "./AuthCard";

const meta: Meta<typeof AuthCard> = {
  component: AuthCard,
};

export const Default: StoryObj<typeof AuthCard> = {};

export default meta;
